#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
SKILL_ROOT="$(cd -- "$SCRIPT_DIR/.." && pwd)"

PYTHONPATH="$SKILL_ROOT${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m cross_agent_consensus.cli "$@"
