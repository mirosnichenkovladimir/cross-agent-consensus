# File-Based MVP Runner

Placeholder for optional automation.

Do not automate model calls until the protocol and profiles have been dogfooded manually.

First useful command later:

```text
consensus init --profile document-consensus --task task.md
```

It should only scaffold a run directory and copy templates.
